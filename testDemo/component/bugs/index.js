import React, { Component } from 'react';
import DataChange from './datachange';

// data in render, padding change error 
export default class Guide extends Component {
  render() {
    return (
      <div>
        <DataChange />
      </div>
    );
  }
}
